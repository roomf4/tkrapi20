#!/bin/bash

# pgcopy.bash

# This script should use pg:copy to copy local db to DATABASE_URL
heroku pg:copy URL1 URL2 --app tkrapi20
# This works if heroku postgres can connect to URL1 and URL2.
# So, unfortunately, neither can be my laptop.
j
exit
